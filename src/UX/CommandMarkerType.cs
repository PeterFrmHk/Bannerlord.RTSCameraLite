namespace Bannerlord.RTSCameraLite.UX
{
    /// <summary>Temporary command / doctrine marker kinds (Slice 19 — no persistent HUD).</summary>
    public enum CommandMarkerType
    {
        GroundTarget = 0,
        MoveTarget = 1,
        ChargeTarget = 2,
        ReformPoint = 3,
        CommanderAnchor = 4,
        RallyPoint = 5,
        Warning = 6
    }
}
